function Rectanglearea(l,h) {
    const area = l * h;
    return area;
}

const l = 10;
const h = 5;

const area = Rectanglearea(l, h);

console.log(`The area of the Rectangle with length =  ${l} and  height = ${h} and ans is =  ${area}`);